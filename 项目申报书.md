# moonsh doctor 项目申报书

## 基本信息

- 参赛者：soehi
- 联系方式：811446351@qq.com
- 仓库：https://github.com/soehi/moonbit
- 方向：MoonBit 开发工具
- 许可证：Apache-2.0
- 类型：已有项目的实质扩展

## 项目目标

为 MoonBit 项目提供跨平台工作区检查工具。开发者在提交仓库或发布包前，可检查 `moon.mod`、`moon.pkg`、源码、测试、README、许可证和 CI 是否齐备，并获得明确的改进提示。

## 与现有项目的关系

Mooncakes 的 `mooxCLI/cmd` 已覆盖常见 Unix 命令，`mizchi/markdown` 已提供 Markdown 解析和渲染。本项目不再以重复实现这些功能为目标；仓库中的旧命令仅保留兼容性。本期重点是在文件能力之上增加 MoonBit 工程结构识别与质量诊断。

## 拟实现功能

1. 扫描 MoonBit 模块、包、源码、测试、文档、许可证和 CI 配置。
2. 检查每个 `moon.pkg` 所在目录是否具有对应测试。
3. 提供 A–F 健康等级及可操作的诊断信息。
4. 同时提供终端文本、JSON、GitHub Markdown 和 README 徽章输出。
5. 将诊断核心设计为可被编辑器、脚手架和 CI 复用的 MoonBit API。
6. 后续解析 `moon.mod`、`moon.pkg` 元数据，并支持可配置的检查规则。

## 预期成果

交付可运行的 `moonsh doctor` 命令、公开 MoonBit API、自动化测试和完整项目文档。项目以 MoonBit 为主要实现语言，并保留连续 Git 提交记录。
