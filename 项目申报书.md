# moonsh 项目申报书（复审版）

## 基本信息

- 项目名称：moonsh doctor：MoonBit 工作区健康诊断工具
- 参赛者：soehi
- 联系方式：811446351@qq.com
- GitHub 仓库：https://github.com/soehi/moonbit
- 项目方向：语言与开发工具 / 工程质量基础设施
- 项目许可证：Apache-2.0
- 本期性质：已有项目的实质扩展

## 一句话介绍

`moonsh doctor` 用纯 MoonBit 扫描一个工作区，检查模块、包、源码、测试、README、许可证与 CI 是否齐备，生成可读、稳定、可在 CI 中复用的健康报告。

## 选题调整与扩展关系

初版申报把 `ls`、`cat`、`grep` 等 Unix 命令及 Markdown 渲染列为主体，与近期维护的 Mooncakes 组件存在明显重叠。复审版主动收窄边界：

| 生态项目 | 已有能力 | moonsh 的边界 |
|---|---|---|
| `mooxCLI/cmd` | 可通过 Mooncakes 获取的通用命令集合 | 不再把重复实现 Unix 命令作为本期成果；这些代码仅作为早期原型和底层文件处理能力保留 |
| `mizchi/markdown` | 完整的 Markdown 解析、HTML/TUI 渲染及扩展 | 不再把 Markdown 解析器作为本期成果；现有 `md` 仅为兼容入口，后续优先复用成熟包 |
| `moonsh doctor` | MoonBit 工程级诊断 | 新增工作：识别 MoonBit 模块和包结构，聚合测试、文档、许可证与 CI 信号，输出类型化健康报告 |

因此，本项目不是再造一组 Mooncakes 命令，而是在文件访问能力之上增加“理解 MoonBit 工作区”的语义层。它既可以作为 CLI 运行，也可以通过 `audit_workspace` API 嵌入编辑器、脚手架或 CI 工具。

## 真实需求

MoonBit 项目在提交开源仓库、发布 Mooncakes 包或参加验收前，通常要人工确认：

- 根目录是否有 `moon.mod`；
- 每个包是否有 `moon.pkg`；
- 是否存在 `.mbt` 源文件和 `_test.mbt` / `_wbtest.mbt` 测试；
- README、开源许可证和 CI 是否齐备；
- Windows 与 Unix 路径是否能得到一致结果。

这些检查分散、容易遗漏，也不适合在不同平台用 Shell 脚本重复维护。`moonsh doctor` 将其实现为确定性的 MoonBit API 与单一命令。

## 本期已完成的实质新增工作

1. 新增 `AuditSeverity`、`AuditFinding`、`WorkspaceAudit` 类型化诊断模型。
2. 新增纯函数 `audit_workspace(files)`：对路径清单执行跨平台、无副作用的工程检查。
3. 新增 `format_workspace_audit`：产生稳定文本，可用于 CI 日志和快照测试。
4. 新增 `moonsh doctor [path]`：异步递归扫描目录，跳过 `.git`、构建目录和依赖目录。
5. 新增 Windows 路径、完整工程、缺失工程要素等测试。
6. 重写 README 与申报书，明确与 Mooncakes CLI/Markdown 的关系及本期新增范围。

## 使用示例

```bash
moon run cmd/main -- doctor .
```

示例输出：

```text
MoonBit workspace health: 100/100
sources=21 tests=2 packages=2
[PASS] module: 找到根目录 moon.mod
[PASS] readme: 找到项目 README
[PASS] license: 找到开源许可证
[PASS] ci: 找到 GitHub Actions 工作流
[PASS] packages: 找到 2 个 MoonBit 包
[PASS] sources: 找到 21 个 MoonBit 源文件
[PASS] tests: 找到 2 个测试文件
```

## 技术设计

- **纯核心、薄 I/O 层**：评分逻辑只接收相对路径数组，文件遍历单独放在 CLI 适配层，容易测试和嵌入。
- **跨平台路径归一化**：Windows 与 Unix 分隔符统一处理，并对诊断所需文件名做大小写归一化。
- **类型化结果**：调用者不需要解析终端文本，可以直接读取分数、文件计数和诊断项。
- **确定性输出**：同一文件集合始终得到同一报告，适合 CI 与回归测试。
- **安全扫描**：默认不进入 `.git`、`.mooncakes`、`_build`、`target` 和 `node_modules`，并限制递归深度。

## 验收方式

```bash
moon check
moon test
moon run cmd/main -- doctor .
moon info
moon fmt
```

验收重点不是旧有通用命令数量，而是：

- `doctor` 能在真实 MoonBit 仓库运行；
- 核心 API 有稳定接口和自动化测试；
- Windows/Unix 路径行为一致；
- README 能清楚复现运行与验证步骤；
- Git 提交能区分本期新增工作。

## 后续计划

- 读取 `moon.mod` / `moon.pkg` 内容，检查依赖和模块元数据；
- 提供 JSON 输出和可配置规则，便于 CI 设置质量门禁；
- 检查公开 API 文档、生成的 `.mbti` 是否同步；
- 把诊断规则拆成可注册的检查器，并与成熟 Mooncakes 组件集成；
- 将旧的 Markdown 兼容实现迁移为对成熟 Markdown 包的适配，避免重复维护。

## 原创性与 AI 使用说明

本期新增的工作区诊断模型、评分规则、扫描适配和测试均在本仓库内以 MoonBit 实现。项目使用 AI 辅助分析、编码和文档整理；参赛者负责选题边界、技术选择、测试验证和最终质量。已有通用命令原型不冒充本期新增成果。
