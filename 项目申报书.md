# moonsh 项目申报书

## 基本信息

- 项目名称：moonsh：用 MoonBit 重写的现代化 Shell 命令行工具集
- 参赛者：soehi
- 联系方式：[811446351@qq.com]
- GitHub 仓库链接：https://github.com/soehi/moonbit.git
- 项目方向：CLI 工具库 / 系统编程工具集
- 是否为原创项目：是
- 项目许可证：Apache-2.0

## 项目简介

moonsh 是一个用 MoonBit 语言编写的现代化 Shell 命令行工具集，包含 14 个日常开发中最常用的 Unix 命令的重新实现。项目灵感来自 Rust 生态中的 bat、eza、fd、ripgrep 等现代化工具，旨在展示 MoonBit 在系统编程领域的潜力，为 MoonBit 生态提供一套功能完整、性能优异的命令行工具。

项目完全使用 MoonBit 原生实现，不依赖外部 C 库。核心亮点包括自研的 glob 匹配引擎、正则表达式引擎和 LCS 差异算法，以及基于 moonbitlang/async 的异步 I/O 实现。所有工具均通过 native 后端编译为机器码，具有快速启动和低内存占用的特点。

## 项目方向与通用性说明

本项目属于 CLI 工具库方向，具有高度通用性：

- **跨平台基础工具**：提供的 14 个命令是 Unix/Linux/macOS/Windows 系统的基础操作工具，任何操作系统用户都需要
- **开发流程必备**：文件搜索(grep/find)、文本处理(sort/uniq/cut/tr)、文件查看(cat/head/tail)是日常开发的核心操作
- **脚本与自动化**：命令行工具是 Shell 脚本、CI/CD 流水线、自动化任务的基础组件
- **MoonBit 生态基础设施**：为 MoonBit 开发者提供原生的系统编程工具，降低生态入门门槛
- **可复用引擎**：glob 匹配、正则引擎、差异算法等可作为独立库被其他 MoonBit 项目引用

## 预期使用场景

### 场景一：日常开发文件操作

开发者使用 moonsh 替代系统自带的 ls、cat、grep 等命令进行日常开发：

```bash
# 列出目录内容，带颜色和图标
moonsh ls -l -c ~/projects

# 搜索包含特定模式的代码文件
moonsh grep -n "TODO" -i src/

# 查看文件内容并显示行号
moonsh cat -n main.mbt

# 比较两个版本的配置文件差异
moonsh diff old.json new.json
```

### 场景二：Shell 脚本与自动化

moonsh 命令可组合使用，构建自动化脚本：

```bash
# 统计代码行数
find . -name "*.mbt" | xargs moonsh wc -l

# 批量重命名文件（通过 cut + sort 组合）
moonsh find -n "*.txt" . | moonsh sort | moonsh uniq

# 处理日志文件
moonsh grep "ERROR" app.log | moonsh cut -d: -f3 | moonsh sort | moonsh uniq -c
```

### 场景三：文档处理与 Markdown 渲染

使用 moonsh 的 md 命令将 Markdown 文档转换为 HTML：

```bash
# 将 README 转为 HTML
moonsh md README.md

# 生成完整 HTML 文档（带 DOCTYPE 和样式）
moonsh md -f -t "项目文档" docs/guide.md

# 从管道读取 Markdown 并渲染
echo "# Hello MoonBit" | moonsh md -
```

## 拟实现的核心功能

### 已实现功能（14 个命令）

| 命令 | 功能 | 对标工具 |
|------|------|---------|
| `ls` | 目录列表（短格式/长格式/树形/颜色/隐藏文件） | exa / eza |
| `cat` | 文件查看（行号/语法高亮/stdin） | bat |
| `find` | 文件搜索（glob 模式/类型过滤/深度控制/隐藏文件） | fd |
| `grep` | 文本搜索（正则/行号/反向/上下文/忽略大小写/stdin） | ripgrep |
| `wc` | 统计（行/单词/字符/最大行长度） | wc |
| `head` | 显示文件开头（行数控制/stdin） | head |
| `tail` | 显示文件末尾（行数控制/stdin） | tail |
| `stat` | 文件信息（类型/大小/时间戳/权限） | stat |
| `sort` | 排序（字典序/数值/自然/版本/逆序/去重/按字段） | sort |
| `uniq` | 去重（计数/忽略大小写/跳过字段/字符） | uniq |
| `cut` | 字段提取（分隔符/字符位置/字节位置） | cut |
| `tr` | 字符转换（转换/删除/压缩/POSIX 字符类） | tr |
| `diff` | 文件比较（LCS 算法/统一格式/彩色输出） | diff |
| `md` | Markdown 渲染（标题/段落/列表/代码块/表格/链接/图片/粗体/斜体/删除线） | pandoc / marked |

### 自研引擎

- **Glob 匹配引擎**：支持 `*`、`?`、`[abc]`、`[a-z]`、`[!abc]`、`**` 跨目录匹配
- **正则表达式引擎**：支持 `.`、`*`、`+`、`?`、`^`、`$`、`[]`、`\d`、`\w`、`\s`、`\b`、`()` 分组，贪心匹配 + 回溯
- **LCS 差异算法**：基于最长公共子序列的文本差异比较，支持统一格式输出
- **Markdown 解析器**：纯 MoonBit 实现，支持标准 Markdown 语法（含表格）

### 测试覆盖

- 黑盒测试：测试公开 API 的输入输出（175 个测试用例）
- 白盒测试：测试内部实现细节
- 覆盖所有命令的核心功能和边界条件

## 原创性说明

本项目为**完全原创项目**，不是移植项目：

- **原创实现**：所有代码均为 MoonBit 原生编写，从零开始设计和实现
- **灵感来源**：项目灵感来自 Rust 生态的 bat、eza、fd、ripgrep 等工具，但并非移植这些项目
- **自研引擎**：glob 匹配、正则表达式、LCS 差异算法均为独立实现，不依赖任何外部 C 库
- **MoonBit 特性**：充分利用 MoonBit 的模式匹配、代数数据类型、trait 系统等现代语言特性
- **异步 I/O**：基于 moonbitlang/async 实现非阻塞文件操作，发挥 MoonBit 的异步编程能力

## 项目结构

```
moonsh/
├── moon.mod              # 模块配置
├── moon.pkg              # 库包配置
├── moonsh.mbt            # 公共工具（颜色/格式化/文件类型/权限/时间戳）
├── glob.mbt              # Glob 匹配引擎
├── regex.mbt             # 正则表达式引擎
├── path.mbt              # 路径处理工具
├── ls.mbt                # ls 命令
├── cat.mbt               # cat 命令
├── find.mbt              # find 命令
├── grep.mbt              # grep 命令
├── wc.mbt                # wc 命令
├── head_tail.mbt         # head / tail 命令
├── stat.mbt              # stat 命令
├── sort.mbt              # sort 命令
├── uniq.mbt              # uniq 命令
├── cut.mbt               # cut 命令
├── tr.mbt                # tr 命令
├── diff.mbt              # diff 命令
├── markdown.mbt          # Markdown 解析器和 HTML 渲染器
├── moonsh_test.mbt       # 黑盒测试
├── moonsh_wbtest.mbt     # 白盒测试
├── cmd/main/
│   ├── main.mbt         # CLI 入口
│   └── moon.pkg         # 可执行包配置
└── README.md
```

## 技术栈

| 组件 | 说明 |
|------|------|
| 语言 | MoonBit v0.1.20260819 |
| 后端 | Native（直接编译到机器码） |
| 异步 I/O | moonbitlang/async@0.21.0 |
| 参数解析 | moonbitlang/core/argparse |
| 字符串处理 | moonbitlang/core/string |
| 许可证 | Apache-2.0 |

## 开发计划

1. **Phase 1（已完成）**：实现核心命令（ls、cat、find、grep、wc、head、tail）
2. **Phase 2（已完成）**：实现扩展命令（stat、sort、uniq、cut、tr、diff）
3. **Phase 3（已完成）**：实现 Markdown 渲染器（md 命令）
4. **Phase 4（进行中）**：完善测试覆盖、优化性能、更新文档
5. **Phase 5（计划中）**：添加更多功能（表格渲染、任务队列、环境变量等）

## GitHub 仓库

- 仓库地址：https://github.com/soehi/moonbit.git
- 当前版本：v0.1.0
- 已提交 commits：5 个（持续增加中）
- 分支：master
- 最近更新：2026-09-11
